```sh
mvn --settings ./maven-settings.xml org.mule.extensions:mule-extensions-archetype-maven-plugin:1.3.1:generate
```

```
[INFO] --- mule-extensions-archetype:1.3.1:generate (default-cli) @ standalone-pom ---
* Enter the name of the extension (empty for default):
my example connector
* Enter the extension's groupId (empty for default):
com.example.myapp
* Enter the extension's artifactId (empty for default):
study-mule-custom-connector
* Enter the extension's version (empty for default):
1.0.0-SNAPSHOT
* Enter the extension's main package (empty for default):
myapp
[INFO] Generating project in Batch mode
```
